一、权限、角色说明
1、权限删除之后，per_role表不再需要保留相关数据，级联删除、更新
2、角色删除之后，per_role表不再需要保留相关数据，级联删除、更新
二、用户、角色
1、用户删除之后，user_role表不再需要保留相关数据，级联删除、更新
2、角色删除之后，user_role不需要删除，用户设置为默认角色即可。只级联更新
三、用户从属
1、理论上讲，此系统中一个用户不允许属于多个用户组
2、为了兼容以后，设计的是用户可以属于多个用户组
3、为了Admin用户正常使用，增加了roleId和RoleName字段，增改查都要进行修改